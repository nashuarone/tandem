import React from "react";
import s from "./ProfileInfo.module.css";

const ProfileInfo = () => {
  return <div className={s.discription}>ava + discription</div>;
};

export default ProfileInfo;
